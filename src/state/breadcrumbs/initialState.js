const breadcrumbs = {
  style: {
    breadcrumbs: `text-decoration: none;
color: var(--color_complementary);
font-size: calc(var(--base_font-size)-4px);`,

    "breadcrumbs:hover": `color: var(--color_desaturated);`
  }
};

export default breadcrumbs;
