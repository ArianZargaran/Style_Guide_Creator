const featuredIcons = {
  style: {
    icon_featured: `icon_featured {
  cursor: pointer;
  background-color: var(--color_main);
  border-radius: 100%;
  font-size: var(--h4_font-size);
}`,
    "icon_featured fas fa-flag fa-stack-1x fa-inverse": `icon_featured {
  /* ICONS IMPORTED FROM FONT-AWESOME */
}`,
    "icon_featured:hover fas fa-terminal fa-stack-1x fa-inverse": `icon_featured:hover {
  background-color: var(--color_complementary);
}`
  }
};

export default featuredIcons;
